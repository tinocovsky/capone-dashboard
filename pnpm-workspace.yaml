packages:
  - "apps/*"
  - "packages/*"
# pnpm 11+ exige allowBuilds explícito; sem isso, sharp/esbuild/core-js ficam
# "ignored builds" e quebram o build no Railway (Nixpacks não roda prompts).
allowBuilds:
  core-js: true
  esbuild: true
  sharp: true

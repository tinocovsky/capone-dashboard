# Nixpacks config (refer to https://nixpacks.com/docs/configuration)
# Node 20 é pego automaticamente via .node-version na raiz do repo.
# nixPkgs fica vazio aqui pra não conflitar com a detecção automática.
